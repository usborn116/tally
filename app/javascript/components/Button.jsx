import React from "react";
import { Link } from "react-router-dom";

export const Button = ({name, endpoint}) => {

    return (
        <>
            <button>Create New {name}</button>
        </>
        

    )

}