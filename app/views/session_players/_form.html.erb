<%= form_with(model: session_player) do |form| %>
  <% if session_player.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(session_player.errors.count, "error") %> prohibited this session_player from being saved:</h2>

      <ul>
        <% session_player.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
