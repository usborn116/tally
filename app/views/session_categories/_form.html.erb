<%= form_with(model: session_category) do |form| %>
  <% if session_category.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(session_category.errors.count, "error") %> prohibited this session_category from being saved:</h2>

      <ul>
        <% session_category.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :point_based, style: "display: block" %>
    <%= form.check_box :point_based %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
